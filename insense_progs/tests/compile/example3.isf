// tests interface definitions with multiple types

type foo is interface( in integer ticker; out integer bob )

component Radio presents foo {
	
	constructor() {
	}
	
	constructor( in integer startticks ) {
		ticker = startticks;
	}
	
	constructor( in integer startticks; integer framesize ) { 
		ticker = startticks;
	}	
	
	behaviour {
		
	}
}
 


